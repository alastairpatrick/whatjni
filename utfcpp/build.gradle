plugins {
    id 'cpp-library'
}

library {
    linkage = [Linkage.STATIC]
    source.from('utf8.cpp')
    publicHeaders.from file('utfcpp/source')

    targetMachines = [
            machines.linux.x86_64,
            machines.windows.x86, machines.windows.x86_64,
            machines.macOS.x86_64
    ]
}
